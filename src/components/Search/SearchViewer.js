import React from "react";
import styled from "styled-components";

const SearchViewer = ({
  movieResults,
  tvResults,
  searchTerm,
  error,
  loading,
  handleSubmit,
}) => {};

export default SearchViewer;
