import React from "react";
import styled from "styled-components";

const SearchViewer = ({
  movieResults,
  tvResults,
  searchTerm,
  error,
  loading,
  handleSubmit,
}) => <div>Search Page</div>;

export default SearchViewer;
